[build]
builder = "NIXPACKS"
watchPatterns = ["src/**", "package.json", "bun.lock"]

[deploy]
startCommand = "bun run src/index.ts"
preDeployCommand = "bun run db:migrate"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
